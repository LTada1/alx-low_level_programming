#include "main.h"
#include <string.h>

/**
 * _puts - Print strings
 * @str: Variable
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
	unsigned int i;
	for (i = 48; i <= strlen(str); i++)
	{
		_putchar(strlen(i);
	}
}
