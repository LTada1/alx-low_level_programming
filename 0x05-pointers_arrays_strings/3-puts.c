#include <stdio.h>
#include<string.h>

/**
 * _puts -  prints a string
 * @*str:argument
 *
 * Description: digits should not be repeated twice
 * Return: Always(0) Success
*/

void _puts(char *str)
{
_puts(str);
_puts("\n");
}
