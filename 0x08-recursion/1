#include "main.h"
#include <stdio.h>

/**
 * _strlen_recursion - return string length
 * @s: string literal
 *
 * Return: Always 0.
 */

int _strlen_recursion(char *s)
{
	int len = 0;
	if (*s == '\0')
	{
		return (0);
	}
	s++;
	len++;
	_strlen_recursion(s)
return (len);
}
